import React from "react";
import CoreLink from "./CoreLink";

const RelativeIndexLink = (props) => {
    return <CoreLink BaseComponent={RelativeIndexLink.BaseComponent} {...props} />;
};

export default RelativeIndexLink;
