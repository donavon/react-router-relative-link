import React from "react";
import CoreLink from "./CoreLink";

const RelativeLink = (props) => {
    return <CoreLink BaseComponent={RelativeLink.BaseComponent} {...props} />;
};

export default RelativeLink;
